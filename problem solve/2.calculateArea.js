// 2. **Calculate Area**: Create a function that calculates and returns the area of a rectangle given its length and width as parameters.



function calculates(length, width){
    const area = length * width;
    return area;
}

console.log(calculates(5,3));


function calculatesRectangleArea(){
    const area = length * width;
    return area;
}

const length = 5;
const width = 3;

const area = calculatesRectangleArea(width, length);
console.log('the ractangle is ' + ''  + area);